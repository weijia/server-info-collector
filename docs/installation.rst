============
Installation
============

At the command line::

    $ easy_install server-info-collector

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv server-info-collector
    $ pip install server-info-collector
